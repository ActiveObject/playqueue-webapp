<div class="audio{{#if queued}} queued {{/if}}"
	data-audio-id="{{ aid }}"
	data-owner-id="{{ owner_id }}">

	<div><span class="title">{{ title }}</span></div>
	<div><span class="artist">{{ performer }}</span></div>
	<div class="add-to-queue">
		<div class="icon-add-2"></div>
	</div>
</div>